export default class User {
    constructor(id, firstName, lastName, city, age) { //kelime anlamı oluşturucu demek 
        //this ==>  User classı demek
        this.id = id
        this.firstName = firstName
        this.lastName = lastName
        this.city = city
        this.age = age
    }
}